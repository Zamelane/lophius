{
  "name": "database",
  "version": "0.1.0",
  "dependencies": {
    "@types/consola": "^2.2.8",
    "consola": "^3.4.2"
  },
  "private": true,
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "push": "drizzle-kit push",
    "full": "bun generate && bun push",
    "studio": "bunx drizzle-kit studio"
  }
}
